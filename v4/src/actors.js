import { StateMachine, angleDelta, approachAngle, clamp, distance, fromAngle, length, normalize, rand, sub } from './core.js';
import { ENEMY_TYPES } from './data.js';
import { AbilitySystem, applyDirectionalHit, buildRunStats, computeMechRig } from './systems.js';
let NEXT_ID=1;
export class PlayerMech{
constructor(mech,modules=[]){this.id=NEXT_ID+=1;this.mech=mech;this.modules=modules;this.stats=buildRunStats(mech,modules);this.x=0;this.y=2.5;this.vx=0;this.vy=0;this.aim=-Math.PI/2;this.yaw=this.aim;this.radius=.62;this.maxHp=this.stats.maxHp;this.hp=this.maxHp;this.dead=false;this.invulnerable=0;this.hitStun=0;this.hitVisual={back:0,side:0};this.markTime=0;this.markAmount=0;this.primaryKick=0;this.primarySpreadVisual=0;this.saberPhase=0;this.saberTimer=0;this.saberHitDone=false;this.secondSlashQueued=false;this.burstRemaining=0;this.burstTimer=0;this.dashTimer=0;this.dashVector={x:0,y:-1};this.dashCharges=(this.stats.dashCharges||1)+(this.stats.effects.dashCharges||0);this.maxDashCharges=this.dashCharges;this.dashRecharge=0;this.dashTrailTimer=0;this.overdrive=0;this.overdriveTimer=0;this.speed01=0;this.ability=new AbilitySystem();}
refreshBuild(modules,preserveHpRatio=true){const ratio=this.maxHp>0?this.hp/this.maxHp:1;this.modules=modules;this.stats=buildRunStats(this.mech,modules);this.maxHp=this.stats.maxHp;this.hp=preserveHpRatio?Math.max(1,this.maxHp*ratio):this.maxHp;this.maxDashCharges=(this.stats.dashCharges||1)+(this.stats.effects.dashCharges||0);this.dashCharges=Math.min(this.maxDashCharges,Math.max(this.dashCharges,1));}
resetForRoom(position={x:0,y:4.5}){this.x=position.x;this.y=position.y;this.vx=0;this.vy=0;this.dead=false;this.hitStun=0;this.saberTimer=0;this.saberPhase=0;this.dashTimer=0;this.invulnerable=.5;this.ability.clear();}
update(dt,input,world){if(this.dead)return;this.ability.tick(dt);this.invulnerable=Math.max(0,this.invulnerable-dt);this.hitStun=Math.max(0,this.hitStun-dt);this.markTime=Math.max(0,this.markTime-dt);this.primaryKick=Math.max(0,this.primaryKick-dt*8);this.hitVisual.back*=Math.max(0,1-dt*12);this.hitVisual.side*=Math.max(0,1-dt*12);this.overdriveTimer=Math.max(0,this.overdriveTimer-dt);this.overdrive=clamp(this.overdrive,0,this.stats.overdriveNeed||100);if(input.pressed.primary)this.ability.request('primary');if(input.held.primary)this.ability.request('primary',.08);if(input.pressed.secondary)this.ability.request('secondary');if(input.pressed.dash)this.ability.request('dash');if(input.pressed.ordnance)this.ability.request('ordnance');if(input.pressed.overdrive)this.ability.request('overdrive');if(length(input.aim)>.12)this.aim=approachAngle(this.aim,Math.atan2(input.aim.y,input.aim.x),this.stats.turnSpeed*dt);this.yaw=approachAngle(this.yaw,this.aim,this.stats.turnSpeed*.72*dt);this.updateDash(dt,input,world);this.updateSecondary(dt,world);this.updatePrimary(dt,world);this.updateOrdnance(world);this.updateOverdrive(world);if(this.dashTimer<=0){const move=length(input.move)>1?normalize(input.move):input.move,stunnedMul=this.hitStun>0?.2:1,speedMul=this.overdriveTimer>0?1.18:1,targetVx=move.x*this.stats.moveSpeed*stunnedMul*speedMul,targetVy=move.y*this.stats.moveSpeed*stunnedMul*speedMul,accel=1-Math.exp(-dt*14);this.vx+=(targetVx-this.vx)*accel;this.vy+=(targetVy-this.vy)*accel;}this.x+=this.vx*dt;this.y+=this.vy*dt;this.x=clamp(this.x,world.bounds.left+this.radius,world.bounds.right-this.radius);this.y=clamp(this.y,world.bounds.top+this.radius,world.bounds.bottom-this.radius);this.speed01=clamp(Math.hypot(this.vx,this.vy)/Math.max(1,this.stats.moveSpeed),0,1);this.rechargeDash(dt);}
updateDash(dt,input,world){const ready=this.dashCharges>0&&this.dashTimer<=0&&this.hitStun<=0;if(this.ability.consume('dash',ready)){const source=length(input.move)>.15?normalize(input.move):fromAngle(this.aim);this.dashVector=source;this.dashTimer=this.stats.dashDuration;this.invulnerable=Math.max(this.invulnerable,this.stats.dashDuration*.8);this.dashCharges-=1;this.dashRecharge=Math.max(this.dashRecharge,this.stats.dashCooldown);if(this.stats.effects.dashReload)this.ability.setCooldown('primary',0);world.audio.play('dash');world.spawnVfx({type:'dashStart',x:this.x,y:this.y,angle:Math.atan2(source.y,source.x),color:this.mech.palette.glow,life:.25});if(this.stats.effects.dashDecoy)world.spawnDecoy({x:this.x,y:this.y,life:1.2});}if(this.dashTimer>0){this.dashTimer=Math.max(0,this.dashTimer-dt);this.vx=this.dashVector.x*this.stats.dashSpeed;this.vy=this.dashVector.y*this.stats.dashSpeed;this.dashTrailTimer-=dt;if(this.dashTrailTimer<=0){this.dashTrailTimer=.035;world.spawnVfx({type:'afterimage',x:this.x,y:this.y,angle:this.aim,color:this.mech.palette.primary,life:.2,alpha:.35});}if(this.stats.effects.dashDamage)world.damageEnemiesInCircle(this,.78,this.stats.effects.dashDamage,{type:'dash',stagger:.22,knockback:3.2});}}
rechargeDash(dt){if(this.dashCharges>=this.maxDashCharges)return;this.dashRecharge-=dt;if(this.dashRecharge<=0){this.dashCharges+=1;if(this.dashCharges<this.maxDashCharges)this.dashRecharge=this.stats.dashCooldown;}}
updatePrimary(dt,world){const canFire=this.saberTimer<=0&&this.dashTimer<=0&&this.hitStun<=0&&this.ability.ready('primary');if(this.burstRemaining>0){this.burstTimer-=dt;if(this.burstTimer<=0){this.firePrimary(world);this.burstRemaining-=1;this.burstTimer=.075;}}if(this.ability.consume('primary',canFire)){const burst=this.stats.effects.primaryBurst||1;this.firePrimary(world);this.burstRemaining=Math.max(0,burst-1);this.burstTimer=.075;this.ability.setCooldown('primary',this.stats.primaryRate*(this.overdriveTimer>0?.62:1));}}
firePrimary(world){const rig=computeMechRig(this,world.time),count=this.stats.effects.primaryCount||1,spread=this.stats.effects.primarySpread||.07,baseDamage=this.stats.primaryDamage*(this.overdriveTimer>0?1.25:1);for(let i=0;i<count;i+=1){const offset=(i-(count-1)/2)*spread,angle=this.aim+offset;world.spawnProjectile({owner:'player',x:rig.muzzle.x,y:rig.muzzle.y,angle,speed:this.stats.projectileSpeed,damage:baseDamage,color:this.mech.palette.glow,life:1.35,radius:.13,pierce:this.stats.effects.primaryPierce||0,ricochet:this.stats.effects.primaryRicochet||0,type:'beam',source:{x:this.x,y:this.y}});}this.primaryKick=1;this.primarySpreadVisual=(Math.random()-.5)*.025;world.spawnVfx({type:'muzzle',x:rig.muzzle.x,y:rig.muzzle.y,angle:this.aim,color:this.mech.palette.glow,life:.12});world.audio.play('beam');}
updateSecondary(dt,world){const canStart=this.saberTimer<=0&&this.dashTimer<=0&&this.hitStun<=0;if(this.ability.consume('secondary',canStart)){this.saberTimer=.42;this.saberHitDone=false;this.secondSlashQueued=Boolean(this.stats.effects.secondaryDouble);world.audio.play('saberCharge');}if(this.saberTimer<=0){this.saberPhase=0;return;}this.saberTimer=Math.max(0,this.saberTimer-dt);this.saberPhase=clamp(1-this.saberTimer/.42,0,1);if(!this.saberHitDone&&this.saberPhase>=.28){this.saberHitDone=true;this.executeSlash(world,false);}if(this.secondSlashQueued&&this.saberPhase>=.76){this.secondSlashQueued=false;this.executeSlash(world,true);}}
executeSlash(world,reverse){const rig=computeMechRig(this,world.time),range=1.2*(this.stats.effects.secondaryRangeMul||1);world.spawnSlash({owner:'player',x:this.x,y:this.y,angle:this.aim,base:rig.saberBase,tip:rig.saberTip,range,width:.35,damage:this.stats.secondaryDamage,reverse,color:this.mech.palette.glow,life:.22,mark:this.stats.effects.secondaryMark?{amount:this.stats.effects.secondaryMark,duration:4}:null});if(this.stats.effects.secondaryWave)world.spawnProjectile({owner:'player',x:rig.saberTip.x,y:rig.saberTip.y,angle:this.aim,speed:11,damage:this.stats.secondaryDamage*.55,color:this.mech.palette.glow,life:.7,radius:.25,pierce:1,type:'wave',source:{x:this.x,y:this.y}});world.audio.play('saber');}
updateOrdnance(world){const canUse=this.dashTimer<=0&&this.hitStun<=0&&this.ability.ready('ordnance');if(!this.ability.consume('ordnance',canUse))return;const rig=computeMechRig(this,world.time),targetCount=this.stats.effects.ordnanceTargets||1,targets=world.getNearestEnemies(this,targetCount,14),extra=this.stats.effects.ordnanceExtra||0,count=Math.max(2,targetCount+extra);for(let i=0;i<count;i+=1){const origin=i%2===0?rig.missileL:rig.missileR;world.spawnMissile({owner:'player',x:origin.x,y:origin.y,target:targets[i%Math.max(1,targets.length)]||null,angle:this.aim+(i%2?.35:-.35),damage:this.stats.ordnanceDamage,color:'#ffb45e',life:4.2,speed:7.2,cluster:this.stats.effects.ordnanceCluster||0,pull:this.stats.effects.ordnancePull||0});}this.ability.setCooldown('ordnance',this.stats.ordnanceCooldown);world.audio.play('missile');}
updateOverdrive(world){const ready=this.overdrive>=this.stats.overdriveNeed&&this.overdriveTimer<=0;if(!this.ability.consume('overdrive',ready))return;this.overdrive=0;this.overdriveTimer=5*(this.stats.effects.overdriveDurationMul||1);this.invulnerable=Math.max(this.invulnerable,.55);world.spawnVfx({type:'overdrive',x:this.x,y:this.y,color:this.mech.palette.glow,life:1.1});if(this.stats.effects.overdriveNova)world.damageEnemiesInCircle(this,5.5,this.stats.effects.overdriveNova,{type:'overdrive',stagger:.55,knockback:5});world.audio.play('overdrive');}
gainOverdrive(amount){this.overdrive=clamp(this.overdrive+amount*(this.stats.effects.overdriveGainMul||1),0,this.stats.overdriveNeed);}
takeDamage(world,damage,source,type='enemy'){if(this.dead||this.invulnerable>0)return 0;const guard=this.saberTimer>0&&this.stats.effects.secondaryGuard,finalDamage=damage*(guard?1-this.stats.effects.secondaryGuard:1);this.hp=Math.max(0,this.hp-finalDamage);this.hitStun=guard?.04:.16;const dx=this.x-source.x,dy=this.y-source.y,d=Math.hypot(dx,dy)||1;this.vx+=dx/d*2.8;this.vy+=dy/d*2.8;this.hitVisual={back:.8,side:Math.sin(Math.atan2(dy,dx)-this.aim)};world.spawnVfx({type:'playerHit',x:this.x,y:this.y,color:'#ff4d66',life:.28});world.audio.play('playerHit');if(world.vibrate)globalThis.navigator?.vibrate?.(guard?20:45);if(this.hp<=0){if(this.stats.effects.absoluteGuard&&guard)this.hp=1;else this.dead=true;}return finalDamage;}}
let NEXT_ENEMY_ID=100;
export class Enemy{
constructor(typeId,x,y,scale=1){const def=ENEMY_TYPES[typeId]||ENEMY_TYPES.grunt;this.id=NEXT_ENEMY_ID+=1;this.def=def;this.type=def.id;this.name=def.name;this.x=x;this.y=y;this.vx=0;this.vy=0;this.yaw=Math.PI/2;this.aim=this.yaw;this.radius=def.radius*scale;this.maxHp=def.hp*scale;this.hp=this.maxHp;this.speed=def.speed;this.damage=def.damage;this.armor=def.armor||0;this.dead=false;this.spawn=.45;this.hitStun=0;this.hitVisual={back:0,side:0};this.markTime=0;this.markAmount=0;this.telegraph=0;this.attackCooldown=rand(.2,.8);this.phase=1;this.phaseAnnounced=1;this.elite=Boolean(def.elite);this.boss=Boolean(def.boss);this.speed01=0;this.saberPhase=0;this.primaryKick=0;this.lastDamageSource=null;this.machine=new StateMachine('spawn',{spawn:{update:(self,dt,time)=>{if(time>=.45)self.machine.set('acquire',self);}},acquire:{enter:(self)=>{self.thinkTimer=rand(.08,.22);},update:(self,dt)=>{self.thinkTimer-=dt;if(self.thinkTimer<=0)self.chooseTactic();}},approach:{update:(self)=>self.updateApproach()},strafe:{update:(self,dt)=>self.updateStrafe(dt)},retreat:{update:(self)=>self.updateRetreat()},telegraph:{enter:(self)=>{self.telegraph=self.telegraphDuration();self.vx*=.25;self.vy*=.25;},update:(self,dt)=>{self.telegraph-=dt;if(self.telegraph<=0)self.performAttack();}},recover:{enter:(self)=>{self.recoverTimer=self.recoveryDuration();},update:(self,dt)=>{self.recoverTimer-=dt;if(self.recoverTimer<=0)self.machine.set('acquire',self);}},hitReact:{enter:(self)=>{self.reactTimer=Math.max(.06,self.hitStun);},update:(self,dt)=>{self.reactTimer-=dt;if(self.reactTimer<=0)self.machine.set('acquire',self);}},dead:{}});}
chooseTactic(){const player=this.world.player;if(!player||player.dead)return;const d=distance(this,player),role=this.def.role;if(this.boss){if(d>6.5)this.machine.set('approach',this);else this.machine.set(Math.random()<.55?'strafe':'telegraph',this);return;}if(['melee','eliteMelee'].includes(role))this.machine.set(d>this.def.range?'approach':'telegraph',this);else if(['sniper','artillery'].includes(role))this.machine.set(d<6?'retreat':(this.attackCooldown<=0?'telegraph':'strafe'),this);else if(role==='tank')this.machine.set(d>this.def.range?'approach':(this.attackCooldown<=0?'telegraph':'strafe'),this);else this.machine.set(d>this.def.range*.9?'approach':(this.attackCooldown<=0?'telegraph':'strafe'),this);}
update(dt,world){this.world=world;if(this.dead)return;this.spawn=Math.max(0,this.spawn-dt);this.hitStun=Math.max(0,this.hitStun-dt);this.markTime=Math.max(0,this.markTime-dt);this.attackCooldown=Math.max(0,this.attackCooldown-dt);this.primaryKick=Math.max(0,this.primaryKick-dt*7);this.saberPhase=Math.max(0,this.saberPhase-dt*2.2);this.hitVisual.back*=Math.max(0,1-dt*11);this.hitVisual.side*=Math.max(0,1-dt*11);if(this.hitStun>0&&!this.machine.is('hitReact','dead'))this.machine.set('hitReact',this);this.machine.update(dt,this);this.x+=this.vx*dt;this.y+=this.vy*dt;this.vx*=Math.max(0,1-dt*7);this.vy*=Math.max(0,1-dt*7);this.x=clamp(this.x,world.bounds.left+this.radius,world.bounds.right-this.radius);this.y=clamp(this.y,world.bounds.top+this.radius,world.bounds.bottom-this.radius);this.speed01=clamp(Math.hypot(this.vx,this.vy)/Math.max(1,this.speed),0,1);this.aim=Math.atan2(world.player.y-this.y,world.player.x-this.x);this.yaw+=clamp(angleDelta(this.yaw,this.aim),-dt*5.5,dt*5.5);if(this.boss){const ratio=this.hp/this.maxHp;this.phase=ratio<.33?3:ratio<.66?2:1;if(this.phase>this.phaseAnnounced){this.phaseAnnounced=this.phase;world.bus.emit('bossPhase',{phase:this.phase,enemy:this});world.spawnVfx({type:'bossPhase',x:this.x,y:this.y,color:this.def.color,life:1.2});this.attackCooldown=0;}}}
moveToward(target,speedMul=1){const dir=normalize(sub(target,this));this.vx+=dir.x*this.speed*speedMul*.18;this.vy+=dir.y*this.speed*speedMul*.18;}
updateApproach(){const p=this.world.player;this.moveToward(p,1);if(distance(this,p)<=this.def.range*.88||this.attackCooldown<=0&&this.def.role==='boss')this.machine.set('telegraph',this);}
updateStrafe(){const p=this.world.player,d=distance(this,p),to=normalize(sub(p,this)),side=this.id%2?1:-1;this.vx+=(-to.y*side+to.x*clamp((d-this.def.range)/4,-.4,.4))*this.speed*.11;this.vy+=(to.x*side+to.y*clamp((d-this.def.range)/4,-.4,.4))*this.speed*.11;if(this.attackCooldown<=0)this.machine.set('telegraph',this);}
updateRetreat(){const p=this.world.player,away=normalize(sub(this,p));this.vx+=away.x*this.speed*.18;this.vy+=away.y*this.speed*.18;if(distance(this,p)>=this.def.range*.72)this.machine.set('strafe',this);}
telegraphDuration(){if(this.boss)return this.phase===3?.48:.62;if(this.def.role==='sniper')return 1.05;if(this.def.role==='artillery')return .82;if(['melee','eliteMelee'].includes(this.def.role))return .48;return .58;}
recoveryDuration(){if(this.boss)return this.phase===3?.45:.7;return .62;}
performAttack(){if(this.dead||this.world.player.dead)return;const world=this.world,role=this.def.role,p=world.player,angle=Math.atan2(p.y-this.y,p.x-this.x);if(['melee','eliteMelee'].includes(role)){this.saberPhase=1;world.spawnEnemySlash(this,angle,this.damage,this.elite?1.7:1.3);world.audio.play('enemySaber');}else if(role==='artillery'){world.spawnHazard({x:p.x,y:p.y,radius:1.25,delay:.75,damage:this.damage,owner:this});world.audio.play('warning');}else if(role==='sniper'){world.spawnProjectile({owner:'enemy',x:this.x,y:this.y,angle,speed:this.def.projectileSpeed,damage:this.damage,color:this.def.color,life:1.8,radius:.16,type:'sniper',source:{x:this.x,y:this.y}});this.primaryKick=1;world.audio.play('enemyShot');}else if(this.boss)this.performBossAttack(angle);else{const count=this.elite?3:1;for(let i=0;i<count;i+=1)world.spawnProjectile({owner:'enemy',x:this.x,y:this.y,angle:angle+(i-(count-1)/2)*.16,speed:this.def.projectileSpeed||9,damage:this.damage*(this.elite?.72:1),color:this.def.color,life:2,radius:.12,type:'enemyBeam',source:{x:this.x,y:this.y}});this.primaryKick=1;world.audio.play('enemyShot');}this.attackCooldown=this.def.attackCooldown*(this.boss?(this.phase===3?.68:this.phase===2?.82:1):1);this.machine.set('recover',this);}
performBossAttack(angle){const world=this.world,roll=Math.random();if(this.phase===1||roll<.34){const count=this.phase===3?11:this.phase===2?9:7;for(let i=0;i<count;i+=1)world.spawnProjectile({owner:'enemy',x:this.x,y:this.y,angle:angle+(i-(count-1)/2)*.14,speed:10.5,damage:this.damage*.62,color:this.def.color,life:2.2,radius:.13,type:'bossSpread',source:{x:this.x,y:this.y}});world.audio.play('enemyShot');}else if(roll<.67){const ring=this.phase===3?18:12;for(let i=0;i<ring;i+=1)world.spawnProjectile({owner:'enemy',x:this.x,y:this.y,angle:i/ring*Math.PI*2+world.time*.2,speed:7.8,damage:this.damage*.48,color:'#ff6aab',life:3,radius:.14,type:'bossRing',source:{x:this.x,y:this.y}});world.audio.play('warning');}else{const targets=this.phase===3?4:3;for(let i=0;i<targets;i+=1){const offset=fromAngle(i/targets*Math.PI*2,1.3);world.spawnHazard({x:world.player.x+offset.x,y:world.player.y+offset.y,radius:1.1,delay:.65,damage:this.damage*.85,owner:this});}world.audio.play('warning');}}
receiveHit(world,hit){const result=applyDirectionalHit(this,hit);if(!result.applied)return result;this.lastDamageSource=hit.owner||null;world.spawnVfx({type:result.killed?'explosion':'enemyHit',x:this.x,y:this.y,angle:Math.atan2(this.y-hit.source.y,this.x-hit.source.x),color:this.def.color,life:result.killed?.65:.22,scale:this.boss?1.8:this.elite?1.3:1});world.spawnDamageNumber(this.x,this.y-.45,result.damage,result.killed);if(result.killed){this.machine.set('dead',this);world.audio.play(this.boss?'bossDown':'enemyHit');}else{this.machine.set('hitReact',this);world.audio.play('enemyHit');}return result;}}
