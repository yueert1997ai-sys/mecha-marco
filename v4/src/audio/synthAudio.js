export class SynthAudio {
  constructor(volume = .65) { this.volume=volume; this.context=null; this.master=null; }
  unlock(){if(this.context)return;const AudioContext=globalThis.AudioContext||globalThis.webkitAudioContext;if(!AudioContext)return;this.context=new AudioContext();this.master=this.context.createGain();this.master.gain.value=this.volume*.18;this.master.connect(this.context.destination);}
  setVolume(value){this.volume=value;if(this.master)this.master.gain.value=value*.18;}
  tone(freq,duration,type='sine',gain=.5,slide=1){this.unlock();if(!this.context||!this.master)return;const now=this.context.currentTime,osc=this.context.createOscillator(),amp=this.context.createGain();osc.type=type;osc.frequency.setValueAtTime(freq,now);osc.frequency.exponentialRampToValueAtTime(Math.max(20,freq*slide),now+duration);amp.gain.setValueAtTime(.0001,now);amp.gain.exponentialRampToValueAtTime(Math.max(.0002,gain),now+.012);amp.gain.exponentialRampToValueAtTime(.0001,now+duration);osc.connect(amp);amp.connect(this.master);osc.start(now);osc.stop(now+duration+.02);}
  noise(duration=.08,gain=.15){this.unlock();if(!this.context||!this.master)return;const frames=Math.ceil(this.context.sampleRate*duration),buffer=this.context.createBuffer(1,frames,this.context.sampleRate),data=buffer.getChannelData(0);for(let i=0;i<frames;i+=1)data[i]=Math.random()*2-1;const source=this.context.createBufferSource(),amp=this.context.createGain();amp.gain.setValueAtTime(gain,this.context.currentTime);amp.gain.exponentialRampToValueAtTime(.0001,this.context.currentTime+duration);source.buffer=buffer;source.connect(amp);amp.connect(this.master);source.start();}
  play(id){const map={beam:()=>{this.tone(620,.07,'sawtooth',.32,1.7);this.tone(1200,.045,'sine',.16,.7);},saberCharge:()=>this.tone(260,.14,'sine',.22,2.4),saber:()=>{this.noise(.11,.16);this.tone(980,.12,'sawtooth',.22,.35);},dash:()=>{this.noise(.12,.17);this.tone(170,.12,'sawtooth',.25,2.2);},missile:()=>{this.tone(130,.16,'square',.22,.55);this.noise(.1,.1);},overdrive:()=>{this.tone(110,.6,'sawtooth',.22,4);this.tone(880,.45,'sine',.13,.5);},enemyShot:()=>this.tone(350,.08,'square',.18,.65),enemySaber:()=>this.tone(540,.13,'sawtooth',.18,.38),warning:()=>this.tone(760,.12,'square',.12,.8),enemyHit:()=>{this.noise(.065,.12);this.tone(150,.08,'square',.14,.6);},playerHit:()=>{this.noise(.12,.2);this.tone(90,.18,'sawtooth',.25,.55);},bossDown:()=>{this.noise(.7,.3);this.tone(120,.8,'sawtooth',.35,.25);},select:()=>this.tone(780,.05,'sine',.1,1.3)};map[id]?.();}
}